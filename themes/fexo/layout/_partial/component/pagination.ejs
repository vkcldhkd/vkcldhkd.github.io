<% if (page.total > 1){ %>
  <div class="pagination">
    <nav class="page-nav">
      <%- paginator({
        prev_text: '<',
        next_text: '>',
        mid_size: 1
      }) %>
    </nav>
  </div>
<% } %>